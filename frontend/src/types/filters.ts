export interface FilterState {
  carWashType: string;
  searchKey: string;
  distanceRange: number;
  amenities: string[];
  washType: string[];
  ratings: number[];
  priceRange: number;
  operatingHours: string[];
}

